# Production: pospass yerine güçlü şifre kullan (openssl rand -base64 24)
# docker-compose.yml POSTGRES_PASSWORD ile aynı olmalı
DATABASE_URL="postgresql://posuser:rv7RAingkwfq@db:5432/limonpos"
# Zoho - Token Al callback adresi api.the-limon.com olmalı (Zoho Console'da bu URI onaylanmalı)
ZOHO_CLIENT_ID=1000.ITFHSTJOKYHUKO14TZLXNULR3IT4OO
ZOHO_REDIRECT_URI=https://api.the-limon.com/api/zoho/callback
ZOHO_REGION=EU
ZOHO_DC=eu
