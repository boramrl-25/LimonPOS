# Railway API service - Backend only.
# Root Directory = (empty) - build context must be repo root for prisma + backend.
# Dockerfile.railway uses COPY backend/ and COPY prisma - both at repo root.

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.railway"

[deploy]
